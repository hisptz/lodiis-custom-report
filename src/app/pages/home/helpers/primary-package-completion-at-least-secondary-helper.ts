import * as _ from 'lodash';

export function evaluationOfPrimaryPackageCompletionAtLeastOneSecondary(
  analyticDataByBeneficiary: any[],
  progranStages: Array<{
    id: string;
    dataElements: string[];
  }>
): string {
  let completed = 'No';
  //logics
  return completed;
}
